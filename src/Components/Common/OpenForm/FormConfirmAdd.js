export const FormConfirmAdd = (emp) => {
    console.log("->"+emp);
    //clubs.map((c)=>console.log(c));
    return (
        <>
        <p>
        Coach was added. 
        </p>
        </>
    );
}
